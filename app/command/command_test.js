'use strict';

describe('CliRemoteApp.command module', function () {

    beforeEach(module('CliRemoteApp.command'));

    describe('command controller', function () {

        it('should ....', inject(function ($controller) {
            //spec body
            var commandController = $controller('CommandController');
            expect(commandController).toBeDefined();
        }));

    });
});
